name: Integration Test (Docker)
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master
    - name: Extract branch name
      shell: bash
      run: echo "##[set-output name=branch;]$(echo ${GITHUB_REF#refs/heads/})"
      id: extract_branch
    - name: Publish to Registry
      uses: Asimmetric/Publish-Docker-Github-Action@${{ steps.extract_branch.outputs.branch }}
      with:
        name: uxi/publish-docker-github-action
        registry: quay.io
        username: ${{ secrets.QUAY_UXI_ROBOT_USER }}
        password: ${{ secrets.QUAY_UXI_ROBOT_PWD }}
        tags: "latest,integration-test,temp"
        check_existing: true
        tag_names: true
